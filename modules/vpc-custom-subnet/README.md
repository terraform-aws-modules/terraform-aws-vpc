# AWS VPC custom subnet sub-module

Terraform sub-module which creates aditional blocks of customized subnets like `public_subnets`, `private_subnets`, `database_subnets` and so on.

## Usage

## Examples

## Requirements

## Providers

## Modules

## Resources

## Inputs

## Outputs
